<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/static/css/list.css">
    <title>리스트</title>
</head>
<body>
    <h1>리스트</h1>
    <a href="write">글쓰기</a>
    <table>
        <thead>
            <tr>
                <th>번호</th>
                <th>제목</th>
                <th>글쓴이</th>
            </tr>
        </thead>
        <tbody>
        <% data.forEach(item => { %>
            <tr>
                <td><%= item.iboard %></td>
                <td><a href="detail?iboard=<%= item.iboard %>"><%= item.title %></a></td>
                <td><%= item.writer %></td>
            </tr>
            <% }); %>
        </tbody>
    </table>
</body>
</html>